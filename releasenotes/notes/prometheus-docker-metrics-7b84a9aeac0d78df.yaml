---
features:
  - |
    Adds variable ``enable_prometheus_docker_metrics`` for enabling the
    built-in Docker Prometheus exporter. It is disabled by default because the
    available metrics and the names of those metrics are in active development
    and may change at any time.
